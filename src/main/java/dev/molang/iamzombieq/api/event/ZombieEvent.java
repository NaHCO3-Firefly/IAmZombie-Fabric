package dev.molang.iamzombieq.api.event;

/**
 * Marker interface for the mod's lifecycle events. Replaces NeoForge's {@code net.neoforged.bus.api.Event} as the
 * base type so the event hierarchy is loader-agnostic.
 *
 * <p>Part of the STABLE public API surface (semver 1.x).
 */
public interface ZombieEvent {
}
